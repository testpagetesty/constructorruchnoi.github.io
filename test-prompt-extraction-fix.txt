ИСПРАВЛЕНИЕ АЛГОРИТМА ИЗВЛЕЧЕНИЯ ПРОМПТОВ
==========================================

🚨 ПРОБЛЕМА:
При генерации промпта для полного сайта алгоритм обрезал важные поля из оригинальных промптов элементов.

ПРИМЕР ПРОБЛЕМЫ:
- Оригинальный chartjs-bar: НАБОР_ДАННЫХ_1, НАБОР_ДАННЫХ_2, МЕТКИ_ОСИ_X, цвета
- В полном сайте: только ЗАГОЛОВОК

✅ ИСПРАВЛЕНИЕ:

🔧 ОБНОВЛЕНЫ ФУНКЦИИ:
1. generateFullSitePromptWithElements()
2. generateOptimizedFullSitePromptWithElements()

🎯 ИЗМЕНЕНИЯ В АЛГОРИТМЕ:

БЫЛО (плохо):
- Останавливался на "Пример:" - обрезал важные поля
- Пропускал ключевые поля: НАБОР_ДАННЫХ, МЕТКИ_ОСИ, ЛИНИЯ_, ТИП_ВЫНОСКИ
- Слишком строгая фильтрация

СТАЛО (хорошо):
- НЕ останавливается на "Пример:", продолжает до реального примера контента
- Ищет специфические поля: НАБОР_ДАННЫХ, МЕТКИ_ОСИ, ЛИНИЯ_, ТИП_ВЫНОСКИ
- Включает ВСЕ важные инструкции без обрезки

🔍 НОВЫЕ ТРИГГЕРЫ ВКЛЮЧЕНИЯ:
- Требуемый формат:
- ТИП:
- ЗАГОЛОВОК:
- СОДЕРЖИМОЕ:
- ФОРМАТ:
- ВАЖНО для
- НАБОР_ДАННЫХ (для графиков)
- МЕТКИ_ОСИ (для графиков)
- ЛИНИЯ_ (для линейных графиков)
- ТИП_ВЫНОСКИ (для callout)

🛑 НОВАЯ ТОЧКА ОСТАНОВКИ:
Останавливается только на строках типа:
"ID: графики" или "ID: текстовые" (реальный пример контента)

📋 ТЕСТ ИСПРАВЛЕНИЯ:

ДО ИСПРАВЛЕНИЯ (chartjs-bar):
```
chartjs-bar (Chart.js столбцы):
  Требуемый формат:
  ТИП: chartjs-bar
  ЗАГОЛОВОК: [название диаграммы]
```

ПОСЛЕ ИСПРАВЛЕНИЯ (chartjs-bar):
```
chartjs-bar (Chart.js столбцы):
  Требуемый формат:
  ТИП: chartjs-bar
  ЗАГОЛОВОК: [название диаграммы]
  НАБОР_ДАННЫХ_1: [Название первого показателя] - [Январь - значение] * [Февраль - значение] * [Март - значение] * [Апрель - значение] * [Май - значение] * [Июнь - значение]
  НАБОР_ДАННЫХ_2: [Название второго показателя] - [Январь - значение] * [Февраль - значение] * [Март - значение] * [Апрель - значение] * [Май - значение] * [Июнь - значение]
  ВАЖНО: Для chartjs-bar элемент использует заголовок и два набора данных. Выбирайте логичные пары показателей для сравнения.
  Примеры логичных пар показателей:
  - Входящий трафик / Исходящий трафик
  - Продажи / Прибыль
  - Новые клиенты / Повторные клиенты
  - Доходы / Расходы
  - Активные пользователи / Неактивные пользователи
  - Конверсии / Отказы
  ВАЖНО: 
  - Используйте обычные звездочки (*) для разделения данных, НЕ экранируйте их обратными слешами
  - Формат НАБОР_ДАННЫХ: "Название - Цвет - Месяц - Значение * Месяц - Значение"
  - Поддерживается до 5 наборов данных (НАБОР_ДАННЫХ_1 до НАБОР_ДАННЫХ_5)
  - Цвет указывайте в формате HEX (#1976d2, #ff6b6b, #4caf50, #ff9800, #9c27b0 и т.д.)
  - Метки оси X указывайте через запятую
  - Примеры цветов: #1976d2 (синий), #ff6b6b (красный), #4caf50 (зеленый), #ff9800 (оранжевый), #9c27b0 (фиолетовый)
```

✅ РЕЗУЛЬТАТ:
Теперь промпты элементов в полном сайте содержат ВСЕ необходимые поля и инструкции!

🧪 ТЕСТИРОВАНИЕ:
1. Выбрать chartjs-bar в настройках полного сайта
2. Сгенерировать промпт 
3. Проверить что есть НАБОР_ДАННЫХ_1, НАБОР_ДАННЫХ_2, все инструкции
4. Проверить что нет примера контента (ID: графики) 